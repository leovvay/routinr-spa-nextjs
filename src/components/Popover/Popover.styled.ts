import styled from 'styled-components';
import MuiPopover from '@mui/material/Popover';

export const PopoverContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export const PopoverRoot = styled(MuiPopover)``;
