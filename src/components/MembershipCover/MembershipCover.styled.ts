import styled from 'styled-components';

import Image from '@components/Image';

// noinspection CssUnknownTarget
export default styled(Image)`
  mask: url(/membership-mask.svg) center center/contain no-repeat;
`;
