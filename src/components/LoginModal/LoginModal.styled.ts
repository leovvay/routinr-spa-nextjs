import styled from 'styled-components';
import Dialog from '@mui/material/Dialog';

// eslint-disable-next-line import/prefer-default-export
export const LoginModalDialog = styled(Dialog)`
  & .MuiPaper-root {
    width: 100%;
    padding: 0 20px;
  }
`;
