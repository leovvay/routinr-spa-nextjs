import styled from 'styled-components';

import Text from '@components/Text';

// eslint-disable-next-line import/prefer-default-export
export const AnimatedTextText = styled(Text)`
  text-decoration: underline;
`;
